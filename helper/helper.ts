export const getBusRootUrl = (): string => {
  const rootUrl = `https://baseride.com/routes/apigeo/routevariantvehicle`;
  return rootUrl;
};

export const getBusArrivalRootUrl = (): string => {
  const rootUrl = `https://baseride.com/routes/api/platformbusarrival`;
  return rootUrl;
};
